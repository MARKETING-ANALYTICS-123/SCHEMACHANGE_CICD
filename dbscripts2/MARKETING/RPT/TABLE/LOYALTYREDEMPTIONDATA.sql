create or replace TABLE "LOYALTY REDEMPTION DATA" (
	"Id" VARCHAR(100),
	"% Transaction includes Redemption" VARCHAR(100),
	"% Revenue from Redemption" VARCHAR(100),
	"% Employee Transaction includes Redemption" VARCHAR(100),
	"% Employee Revenue from Redemption" VARCHAR(100),
	"% Non-Employee Transaction includes Redemption" VARCHAR(100),
	"% Non-Employee Revenue from Redemption" VARCHAR(100)
);